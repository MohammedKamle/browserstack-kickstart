# browserstack-examples-jasmine-selenium-webdriver
Example Repository for running tests using Jasmine and Selenium Webdriver</br>
Provide your username and accessKey in ```config.json``` file</br>
Run the  command ```jasmine test-runner.js``` to execute spec files in parallel
